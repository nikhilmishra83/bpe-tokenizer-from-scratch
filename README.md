# Byte Pair Encoding Tokenizer 

This repository documents the process of building a Byte Pair Encoding (BPE) tokenizer from scratch in Python.

Planned features:
- character-level initialization
- pair frequency merging
- subword vocabulary learning
- encoding using merge rules

This project continues the journey started in the Word Tokenizer project.
